import {styled} from "styled-components"
import { widthSideBar } from "../../assets/js/avaribale"

export const OverLayLoadding = styled.div`
    display: flex;
    align-items: center;
    justify-content: center;
    position: fixed;
    inset: 0 0 0 ${widthSideBar}px;
    background-color: rgba(165, 165, 161, 0.44)
`
export const PhotoLoadding = styled.div`

`